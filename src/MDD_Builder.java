import java.util.List;

public class MDD_Builder {
    /*
    public static MDD Build(Graph graph, Vertex start, List<Path> paths){
        if(paths.size() == 0)
            return null;
        MDD mdd = new MDD(graph, start);
        MDD_Vertex mdd_start = new MDD_Vertex(start, mdd, 0);

        for(Path curr : paths){

        }

    }
    */
}
